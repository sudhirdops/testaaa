# Unique name for this workflow
name: Deploy master branch to production org

# Definition when the workflow should run
on:
    push:
      branches: [ main ]
      paths:
        - 'force-app/**'
            

# Jobs to be executed
jobs:
    deploy-branch-to-production-org:
        runs-on: ubuntu-latest
        if: ${{ github.actor != 'dependabot[bot]' }}
        steps:
            # Install Salesforce CLI
            - name: 'Install Salesforce CLI'
              run: |
                  wget https://developer.salesforce.com/media/salesforce-cli/sfdx/channels/stable/sfdx-linux-x64.tar.xz
                  mkdir ~/sfdx
                  tar xJf sfdx-linux-x64.tar.xz -C ~/sfdx --strip-components 1
                  echo "$HOME/sfdx/bin" >> $GITHUB_PATH
                  ~/sfdx/bin/sfdx version

            # install SFDX-Git-Delta plugin - https://github.com/scolladon/sfdx-git-delta
            - name: 'Installing sfdx git delta'
              run: | 
                  echo y | sfdx plugins:install sfdx-git-delta
                  sfdx plugins 

            # Checkout the source code
            - name: 'Checkout source code'
              uses: actions/checkout@v2
              with:
                fetch-depth: 0

            # Store secret for production org
            - name: 'Populate auth file with SFDX_URL secret of the integration and staging orgs'
              shell: bash
              run: |
                  echo ${{ secrets.SFDX_PRODUCTION_URL}} > ./SFDX_PRODUCTION_URL.txt

            - name: 'Create delta packages for new, modified or deleted metadata'
              run: | 
                  mkdir changed-sources
                  sfdx sgd:source:delta --to "HEAD" --from "HEAD^" --output changed-sources/ --generate-delta --source force-app/

            # Authenticate to org
            - name: 'Authenticate to Production Org'
              run: sfdx force://3MVG9wt4IL4O5wvI1iJzo..8HMWNJpcSBj1o9DzHetydPkCzpw_Lmo1MYhgb07OWSkt6Ey1X947xEgeXoAmHB:78E0A6F0EF50043299BC03E4CD407C613997986EDD82BF9BA50C5E312C5BEA94:6Cel800D5i000003Moxk8885i000000oj7QTlNH6CKuE0rIkK6R3eYX3EvWCVrOPKvBduyPe9UGezsFea5PESxq6Uu1skMNcCexHUMhRY7S@https://login.salesforce.com/ -f./ SFDX_PRODUCTION_URL.txt -s -a production
              
            - name: 'Authentication of the account'
              run: sfdx config:set defaultusername=sudhirbds-xljp@force.com
          
              
            - name: 'Deploy the entire branch to Production org'
              run: sfdx force:source:deploy -p force-app --testlevel RunLocalTests --json
         
              
              ##- name: 'Deploy destructive changes (if any) to Production org'
              ##run: sfdx force:mdapi:deploy -d -u dopssudhir@salesforcedev.com "changed-sources/destructiveChanges" --checkonly --ignorewarnings 
